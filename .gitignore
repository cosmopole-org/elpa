# Build artifacts (any crate, including nested example crates).
target/
**/target/
**/dist/
**/*.rs.bk
Cargo.lock.bak
.DS_Store
*.swp

# wasm-bindgen output for the web example (build artifact).
**/pkg/

# Node / Playwright e2e harness (scripts/web-e2e.js) — installed locally, not vendored.
node_modules/

# Claude Code agent worktree metadata (session-local, not source).
.claude/
